procedure main() returns integer {
    // tests basic if statements
integer x = 0;
integer y = 1;

    if(x==0){
        x ->std_output;
    }
    if(y != 1){
        y ->std_output;
    }

  /* must have a return */
  return 0;
}