procedure main() returns integer {
  // tests only boolean input
  var boolean x;
  character y;
  x <- std_input;
  x -> std_output;
  y <- std_input;
  x <- std_input;
  x -> std_output;
  

  /* must have a return */
  return 0;
}