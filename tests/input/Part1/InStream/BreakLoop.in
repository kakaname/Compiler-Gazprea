procedure main() returns integer {
/* Prints every number between 1 and 10 */
integer x = 0;
character y;
loop while x < 10 {

  x <- std_input; 
  y <- std_input;
  
}
  x -> std_output;
  

  /* must have a return */
  return 0;
}