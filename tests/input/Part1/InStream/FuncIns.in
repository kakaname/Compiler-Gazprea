procedure main() returns integer {
  // test both tests with single statement and 
  // block statements
  integer x = 2;
  integer out =   times_two(x);
  out -> std_output;
  out =   times_four(x);
  out -> std_output;

  /* must have a return */
  return 0;
}

function times_two(integer x) returns integer <- std_input;

function times_four(integer x) returns integer{
  return <- std_input;
}