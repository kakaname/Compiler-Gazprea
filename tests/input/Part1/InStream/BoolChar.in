procedure main() returns integer {
  // tests only boolean input
  var boolean x;
  var character z;
  x <- std_input;
  x -> std_output;
  z <- std_input;
  x <- std_input;
  x -> std_output;
  
  z <- std_input;
  z <- std_input;
  z -> std_output;
  z <- std_input;
  z -> std_output;


  

  /* must have a return */
  return 0;
}