procedure main() returns integer {
  // tests only boolean input
  var boolean x;
  x <- std_input;
  x -> std_output;
  x <- std_input;
  x -> std_output;
  var character z;
  z <- std_input;
  z -> std_output;
  z <- std_input;
  z -> std_output;


  

  /* must have a return */
  return 0;
}