procedure main() returns integer {
  // tests for character input
  var character x;
  x <- std_input;
  x -> std_output;
  x <- std_input;
  x -> std_output;
  

  /* must have a return */
  return 0;
}