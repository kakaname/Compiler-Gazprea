procedure main() returns integer {
/* Prints every number between 1 and 10, except for 7 */
integer x = 10
character z;
x -> std_output;
loop {
    x <- std_input; 
    z <- std_input;
    x -> std_output;

}while x == 0;
  

  /* must have a return */
  return 0;
}