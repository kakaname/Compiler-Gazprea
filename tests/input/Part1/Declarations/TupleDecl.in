procedure main() returns integer {
// tuple declarations
tuple(integer, real) t1;
tuple(integer a, real) t2;
tuple(integer, real a) t3;
tuple(integer b, real a) t4;
tuple(integer b, real a) t5 = (0,1.0);
tuple(real b, real a) t6 = t5;
t1 = (null, null);
t2 = (identity, identity);

  /* must have a return */
  return 0;
}