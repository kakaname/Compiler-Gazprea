procedure main() returns integer {
  // simply declares a character
var character a;
const character b = 'b';
var character c;
const character d = 'd';

var character e = identity;
const character f = identity;
var character g = null;
const character h = null;

  a = 'a';
  c = 'c';

  /* must have a return */
  return 0;
}