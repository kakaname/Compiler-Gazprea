procedure main() returns integer {
    var x = identity + 2 + identity;
    var y = null + 3.0 + null;
    var z = null + 3.2 + identity;
    x -> std_output;
    y -> std_output;
    z -> std_output;

  /* must have a return */
  return 0;
}