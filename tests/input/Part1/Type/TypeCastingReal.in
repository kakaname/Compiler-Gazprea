procedure main() returns integer {

  boolean x = true;
  character z = 'a';
  integer t = 1;

  real y = as<real>(x);
  y -> std_output;

  x = false;
  y = as<real>(x);
  y -> std_output;

  y = as<real>(z);
  y -> std_output;

  y = as<real>(z);
  y -> std_output;

  y = as<real>(null + 1);

  /* must have a return */
  return 0;
}