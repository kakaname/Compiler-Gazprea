
procedure main() returns integer {
  integer x;
  char y;
  integer z;
  
  x <- std_input;  
  stream_state(std_input) -> std_output;
  y <- std_input;  
  stream_state(std_input) -> std_output;
  z <- std_input;  
  stream_state(std_input) -> std_output;

  return 0;
}