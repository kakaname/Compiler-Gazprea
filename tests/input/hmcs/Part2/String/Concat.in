procedure main() returns integer {
  // vector decl
    
    ['a','b'] || "cd" -> std_output;
    "ef" || 'u' -> std_output;
    '\n' -> std_output;
    "ab" || "cd" || "ef" || 'u' -> std_output;

  /* must have a return */
  return 0;
}