procedure main() returns integer {
  // vector decl
   // base
   integer[1] a;
   boolean[1] b;
   character[1] c;
   real[1] d;

   // size inf
   integer[*] a1 = [1,2];
   boolean[*] b1 = [true, false];
   character[*] c1 = ['a','b'];
   real[*] d1 = [1.2,1.3];

   // type inf
    var a2 = a1 + 1;
    var b2 = not b1;
    var d2 = d1 * 2;

    // expr size
   integer[3-1] a3 = [1,2];
   boolean[1+1] b3 = [true, false];
   character[3-1] c3 = ['a','b'];
   real[1+1] d3 = [1.2,1.3];

   integer[*] a4 = [];

  /* must have a return */
  return 0;
}