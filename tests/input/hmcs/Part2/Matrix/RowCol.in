procedure main() returns integer {
  // vector decl
   // base
   integer[2,3] a = [[1,2]];

    rows(a) -> std_output;
    '\n' -> std_output;
    columns(a) -> std_output;

  /* must have a return */
  return 0;
}