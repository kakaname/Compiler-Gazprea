procedure main() returns integer {

  integer x = 353;
  boolean z = false;
  character y = as<character>(x);
  y -> std_output;

  x = 97;
  y = as<character>(x);
  y -> std_output;

  y = as<character>(z);
  y -> std_output;


  /* must have a return */
  return 0;
}