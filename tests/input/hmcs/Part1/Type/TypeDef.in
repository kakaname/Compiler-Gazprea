typedef integer int;
typedef real float;

procedure main() returns integer {
    const int a = 1;
    var int b = 2;
    const float c = 3;
    var float d = 4;
    const integer e = 5;
    var integer f = 6;
    var int g = null;
    var float h = null + identity + null + identity;

    a -> std_output;
    b -> std_output;
    c -> std_output;
    d -> std_output;
    e -> std_output;
    f -> std_output;


  /* must have a return */
  return 0;
}