procedure main() returns integer {
  // tests for integer input
  var integer x;
  character z;
  x <- std_input;
  x -> std_output;
  z <- std_input;
  x <- std_input;
  x -> std_output;
  z <- std_input;
  x <- std_input;
  x -> std_output;
  

  /* must have a return */
  return 0;
}