procedure main() returns integer {
    // tests basic if statements
integer x = 0;
integer y = 1;
integer z = null + null + null + null;

    if(x==z){
        x ->std_output;
    }
    if(y != 1){
        y ->std_output;
    }

  /* must have a return */
  return 0;
}