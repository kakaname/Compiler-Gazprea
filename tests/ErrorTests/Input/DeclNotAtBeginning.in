typedef integer itype;
typedef character ctype;
typedef real rtype;
typedef boolean btype;
typedef tuple(itype, ctype ctype, btype) ttype;

typedef rtype yo;

procedure main() returns itype {
    yo yo = 15;
    yo = yo + 1.1;
    yo -> std_output;

    const ctype b = 'l';
    b -> std_output;

    var btype c = identity;
    c -> std_output;

    ttype ttype;
    ttype.ctype = 't';
    ttype.ctype -> std_output;

    return 0;

}
