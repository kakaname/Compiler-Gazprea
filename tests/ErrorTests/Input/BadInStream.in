procedure main() returns integer {
  // tests for correct token consumption from spec

    integer i;
    character c;
    i <- std_input;
    i <- std_input;
    c <- std_input;

    i -> std_output;
    c -> std_output;

// whitespace
    c <- std_input;
    c -> std_output;

// whitespace
    c <- std_input;
    c -> std_output;

// whitespace
    c <- std_input;
    c -> std_output;

// 1
    c <- std_input;
    c -> std_output;

// 0
    c <- std_input;
    c -> std_output;

// a
    c <- std_input;
    c -> std_output;

    '$' ->

  return 0;
}